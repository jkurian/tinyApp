<!DOCTYPE html>
<html lang="en">

<head>
    <% include ./partials/_head %>
</head>

<body>
    <header>
        <% include ./partials/_header %>
    </header>
    <table class="table table-nonfluid" style="width: auto;">
        <thead class="thead thead-dark">
            <tr>
                <th>URLs</th>
                <th>Shortened URLs</th>
            </tr>
        </thead>
        <tbody>
            <% for(let url in urlList) { %>
                <tr>
                    <td>
                        <%= urlList[url] %>
                    </td>
                    <td>
                        <%= url %>
                    </td>
                    <td>
                        <a href="/urls/<%= url %>">Edit</a>
                    </td>
                    <td>
                        <form id="deleteURL" method="POST" action="/urls/<%= url %>/delete">
                            <button type="submit" form="deleteURL">Delete URL
                                <i class="icon-trash icon-small"></i>
                            </button>
                        </form>
                    </td>
                </tr>
                <% }; %>
        </tbody>
    </table>
</body>

</html>